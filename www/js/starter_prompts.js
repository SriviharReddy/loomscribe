// No built-in starter prompts in the public build.
// Import your own prompt cards via the in-app "Create Prompt → Import from File" feature.
export const starterPrompts = {
    categories: {},
    contents: {}
};
