{
  "name": "vibechat",
  "version": "1.0.0",
  "description": "VibeChat — Modular Serverless Vanilla HTML/CSS/JS Storyteller",
  "main": "app.js",
  "scripts": {
    "dev": "node server.js",
    "cap:sync": "npx cap sync",
    "cap:copy": "npx cap copy",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
